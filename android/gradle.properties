org.gradle.jvmargs=-Xmx1536m
android.useAndroidX=true
# The project is plain Java: no Kotlin plugin, no new DSL. Same switches as the
# other Android builds on this machine, so AGP 9 behaves the way it did there.
android.builtInKotlin=false
android.newDsl=false
android.nonTransitiveRClass=true
